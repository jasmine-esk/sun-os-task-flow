# Sun OS · Sunline · Create-task flow

Prototype + spec for Wayeez (design review).

Live URL: see Pages settings → deployed link.

Contains:
- **Spec** tab — every state in the create-task flow (Q1–Q5 quick path, F1–F12 full path, S1–S3 Sunrun extras, E1–E7 edge cases) with mock UI per state.
- **Prototype** tab — working clickable demo. Three guided demos (Quick / Full / Sunrun paths), plus full manual interaction with the Sunline canvas. Click any task in a column to open the task-detail panel (Thread / Info / Activity / Connections tabs).
